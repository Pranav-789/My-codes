#include<stdio.h>

int main () {
    int a;
    scanf("%d", &a); // scanf is used to take input and "&"is used to point the adress of variable used
    printf("The value of a is %d", a);
    return 0;
}