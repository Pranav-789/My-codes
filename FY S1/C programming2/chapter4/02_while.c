#include<stdio.h>

int main () {
    int i = 0;
    while(i<4);
    // if condition in while is true then it is executed
    // when condition is false loop will terminate (here at i = 3 is last and i=4 is not executed)

    printf("Happy Birthday"); 
    i++; //i = i + 1;

    return 0;
}