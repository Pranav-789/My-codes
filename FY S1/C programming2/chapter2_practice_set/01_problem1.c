#include<stdio.h>

int main () {
    int a = 1; // we defined a as 1
    int  b = a; // we defined b as a
    int v = 3*3; //we defined v as 3*3 which is 9 
    // all above staements had no problems
    // char dt = '21 dec 2020'; // note char can only store one charcter only so this is wrong

    return 0;
}