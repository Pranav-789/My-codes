#include<stdio.h>

int main () {
    // to chesc if 97 can divide by any number or not
    int a = 234563;
    printf("the value of a%97 is %d", a%97); // if a is divisible by 97 then answer will be 0 if not it will give remainder
    
    return 0;
}