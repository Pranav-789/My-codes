#include<stdio.h>

struct cl_list{
    char name[20];
    int age;
    struct cl_list *link;
};

struct cl_list createlist(int n){

};

int main () {
    struct cl_list *head = NULL, *temp = NULL, *new_node = NULL;
    return 0;
}